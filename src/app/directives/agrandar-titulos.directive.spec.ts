import { AgrandarTitulosDirective } from './agrandar-titulos.directive';

describe('AgrandarTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
